#### Introduction
Frequency pattern is often used with an array/list or a string, 
and allows one to avoid nested loops (quadratic time complexity O(n²)). It helps to reduce nested loop to a linear form


###### Question 1: 
Write a function called sameSquared which accepts two arrays
The function should return true if every value in the first array has its corresponding value squared in the second array
The frequency of the values must be the same
Kindly see the FrequencyCounter.java class for solution
E.g sameSquared([1, 2, 3], [4, 1, 9]); _// true_